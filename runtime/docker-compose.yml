version: '2.1'
services:
  broker:
    container_name: webhookee-broker
    image: melthaw/webhookee:broker
    depends_on:
      - mongodb
      - rabbitmq
    links:
      - mongodb
      - rabbitmq
    ports:
      - "80:80"
    environment:
      MONGODB_HOST: mongodb
      MONGODB_PORT: 27017
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USERNAME: guest
      RABBITMQ_PASSWORD: guest
  management:
    container_name: webhookee-management
    image: melthaw/webhookee:management
    depends_on:
      - mongodb
    links:
      - mongodb
    ports:
      - "8080:80"
    environment:
      MONGODB_HOST: mongodb
      MONGODB_PORT: 27017
  rabbitmq:
    container_name: webhookee-rabbitmq
    image: rabbitmq:3-management
    ports:
      - "15672:15672"
  mongodb:
    container_name: webhookee-mongodb
    image: mongo
    ports:
      - "27017:27017"
